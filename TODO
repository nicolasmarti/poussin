(* calculus_misc should populate himself during the process *)
0) pprinter / parser
   + test1.p passed line by line in regression
   - Enhanced
1) calculus_substitution
   + shifting of terms
   + shift substitution
   + term_substitution
2) reduction
   + added reduced flag to terms
   + is_clean_term, pattern_match, reduction_term 
   - need to be tested
3) unification
   - need to do App + conversion hyp uses
4) terminfer
   - need to do App next
5) termcheck

